/*

        LECTURE 1: REACT ROADMAP AND FUNDAMENTALS


THE GOAL:
- Understand why React exists, when to use it, and the roadmap 
  for learning it effectively

THE APPROACH (Core Concepts & History):
- React was created to solve Facebook's "Ghost Message" problem, 
  where the JavaScript state (variables) and the UI (DOM) were 
  failing to stay in sync
- It is a Library (offers structural freedom like a "cool dude") 
  rather than a Framework (enforces strict rules like the "military")
- React is best utilized for building complex frontends where a 
  single user action updates multiple UI elements, rather than 
  using it as an over-engineered solution for simple web pages
- Prerequisites: Complete mastery of JavaScript basics, execution 
  contexts, and DOM manipulation is necessary before starting

THE ECOSYSTEM & BEYOND:
- React is solely a UI library; it does not include built-in 
  routing, requiring packages like React Router DOM
- It lacks native global state management, which introduces the 
  need for libraries like Redux, Zustand, or Context API
- Because standard React apps have poor SEO, production apps often 
  rely on React-based frameworks like Next.js or Remix
 */